var nativeTrixel = {

Main: function() {
  var trixelEditor = Elm.fullscreen(Elm.Trixel.Main, {
    //setWindowDimensions: { x: 0, y: 0 },
  });
  
  //trixelEditor.ports.setWindowDimensions.send(
  //    {x: window.innerWidth, y: window.innerHeight}
  //);
},

};